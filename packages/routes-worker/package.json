{
	"name": "@genshin-artifacts/routes-worker",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"backup:pull": "node scripts/backup-sync.ts pull",
		"backup:push": "node scripts/backup-sync.ts push"
	},
	"dependencies": {
		"nanoid": "5.1.15"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.16.18",
		"wrangler": "4.103.0"
	}
}
